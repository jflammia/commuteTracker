# Receiver configuration
RECEIVER_HOST=0.0.0.0
RECEIVER_PORT=8080

# Raw data storage path (absolute path on NAS)
RAW_DATA_DIR=./raw

# Home location (lat, lon, radius in meters)
HOME_LAT=0.0
HOME_LON=0.0
HOME_RADIUS_M=150

# Work location (lat, lon, radius in meters)
WORK_LAT=0.0
WORK_LON=0.0
WORK_RADIUS_M=150
